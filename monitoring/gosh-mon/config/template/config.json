{
  "ipfs": "${ipfs_address}",
  "primary-network": "${prim_network}",
  "networks": {
    "${prim_network}": {
      "user-wallet": {
        "pubkey": "${pubkey}",
        "secret": "${secret}"
      },
      "endpoints": ["${conf_endpoint}"]
    }
  }
}
