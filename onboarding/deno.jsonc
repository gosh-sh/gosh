{
    "fmt": {
        "files": {
            // disable deno fmt, use prettier for consistency
            "exclude": ["."]
        }
    },
    "tasks": {
        "fmt": "npx prettier -w .",
        "docker": "docker buildx build -t gosh-onboarding .",
        "supabase_types": "mkdir -p src/db && npx supabase gen types typescript --db-url $SUPABASE_ADMIN_URI > src/db/types.ts"
    }
}
